import React from 'react';

const Mutable_Immutable_Datatype = () => {
  return (
    <div className="bg-gray-100 min-h-screen">
      <nav className="bg-blue-500 text-white p-4">
        <h1 className="text-lg font-bold"> Mutable And Immutable Datatypes</h1>
      </nav>
      <main className="container mx-auto px-4 py-8">
        <h2 className="text-xl font-bold mb-4">What are Mutable Immutable Datatypes in ?</h2>
        <p className="mb-4">
          Mutable Immutable Datatypes are containers for storing data values in . A Mutable_Immutable_Datatype is declared with the <code>var</code>, <code>let</code>, or <code>const</code> keyword, followed by a Mutable_Immutable_Datatype name. The value of the Mutable_Immutable_Datatype can be assigned using the assignment operator (<code>=</code>).
        </p>
        <h2 className="text-xl font-bold mb-4">Declaring Mutable Immutable Datatypes in </h2>
        <p className="mb-4">
          In , Mutable Immutable Datatypes can be declared using the <code>var</code>, <code>let</code>, or <code>const</code> keyword. The <code>var</code> keyword was traditionally used to declare Mutable Immutable Datatypes, but <code>let</code> and <code>const</code> were introduced in ES6 and are now the preferred way to declare Mutable Immutable Datatypes.
        </p>
        <p className="mb-4">
          <code>var</code> is function-scoped, meaning that a Mutable_Immutable_Datatype declared with <code>var</code> is only accessible within the function in which it is declared, or globally if declared outside a function. <code>let</code> and <code>const</code> are block-scoped, meaning that a Mutable_Immutable_Datatype declared with <code>let</code> or <code>const</code> is only accessible within the block in which it is declared.
        </p>
        <h2 className="text-xl font-bold mb-4">Assigning values to Mutable Immutable Datatypes</h2>
        <p className="mb-4">
          In , values can be assigned to Mutable Immutable Datatypes using the assignment operator (<code>=</code>). For example:
        </p>
        <pre className="bg-gray-400 text-white p-3 rounded-lg mb-4 w-full overflow-auto">
          <code>var x = 10; // declares a Mutable_Immutable_Datatype x and assigns the value 10</code>
        </pre>
        <p className="mb-4">
          You can also assign the value of one Mutable_Immutable_Datatype to another Mutable_Immutable_Datatype, like this:
        </p>
        <pre className="bg-gray-400 text-white p-3  rounded-lg mb-4 w-full overflow-auto">
          <code>var x = 10;<br/>var y = x; // assigns the value of x to y</code>
        </pre>
        <h2 className="text-xl font-bold mb-4">Naming Mutable Immutable Datatypes in </h2>
        <p className="mb-4">
          Mutable_Immutable_Datatype names in  can contain letters, digits, underscores, and dollar signs. They must begin with a letter, underscore, or dollar sign. Mutable_Immutable_Datatype names are case-sensitive.
        </p>
      </main>
    </div>
  );
};

export default Mutable_Immutable_Datatype;
